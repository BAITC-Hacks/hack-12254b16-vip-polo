"use client";
import type { InitiativePickerProps } from "@/shared/ports";

export function InitiativePicker({ direction }: InitiativePickerProps) {
  return <section aria-label="Выбор мероприятия"><p>Выбор мероприятия ещё не реализован ({direction}).</p></section>;
}
