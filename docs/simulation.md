# Модуль участника 2

getGameData проверяет форму JSON и возвращает независимую копию. validateScenario/simulateScenario возвращают NOT_IMPLEMENTED; POST /api/simulate — HTTP 501.

Далее реализовать контракт: бюджет/пять направлений, пересчёт baseline, clamp после суммирования, trace/facts/Score и серверную проверку. tests/fixtures нельзя использовать вместо расчёта. Свои тесты — tests/simulation. Обновление общих стартовых проверок согласовать с Талгатом.
